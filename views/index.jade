extends layout

block content
  h4 Here, have some blog posts.
  ul.posts
    each post in postList
      li(style="border-color: #{post.color}")
        h4
          a(href=post.url)=  post.title
        p.unescape= post.preview
